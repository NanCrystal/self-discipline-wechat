import React, { useRef, useEffect, useState } from 'react';
import styles from './index.less';

interface PropsType {}

const DocumentPage: React.FC<PropsType> = (props) => {
  return <div className={styles['document-wrapper']}>
    dfsssssssssssssss
  </div>;
};

export default DocumentPage;
