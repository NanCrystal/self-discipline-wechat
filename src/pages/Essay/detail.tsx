import React, { useRef, useEffect, useState } from 'react';
import styles from './index.less';

interface PropsType {}

const EssayDetailPage: React.FC<PropsType> = (props) => {
  return <div className={styles['essay-detail-wrapper']}>
    {/* div.className */}
  </div>;
};

export default EssayDetailPage;
